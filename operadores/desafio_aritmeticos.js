// Fórmula: (F - 32) * (5 / 9) = C°
// OU
// Fórmula: ((F - 32) / 9 * 5) = C°

let f = 73;
let c = (f - 32) * (5 / 9);

console.log("O valor da conversão é: " + c + " °C");

c = ((f - 32) / 9 * 5);
console.log("O valor da conversão é: " + c + " °C");